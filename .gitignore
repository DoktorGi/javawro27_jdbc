# 1. dodaję do gitigonore ale:
#  -- pusty (parametry są, ale bez wartości)
# 2. drugi commit, dopisania linia poniżej, żeby dowolne zmiany, które zachodzą później
# (m.in. dopisanie wartości) nie były zapisane
# na git (wyciek hasełek)

/src/main/resources/jdbc.properties
/.idea
/target/
